# Project Instructions
- Build with plain PHP (no framework), MySQL, Tailwind CSS, JavaScript.
- Use MVC structure and API endpoints for CRUD/data operations.
- Prefer secure defaults: prepared statements, password hashing, session auth.
